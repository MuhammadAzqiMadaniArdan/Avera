import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["loremflickr.com","placehold.co","res.cloudinary.com"], // tambahkan domain eksternal di sini
    
  },
};

export default nextConfig;
